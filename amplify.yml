version: 1
applications:
  - appRoot: frontend
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: dist/client
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
    backend:
      phases:
        build:
          commands:
            - echo "✅ No backend detected, skipping backend build"
    deploy:
      phases:
        preDeploy:
          commands:
            - echo "🚀 Preparing for deployment"
        deploy:
          commands:
            - if [ "$AWS_EXECUTION_ENV" ]; then npm run start:aws; else npm run start; fi
        postDeploy:
          commands:
            - echo "✅ Deployment completed successfully!"
